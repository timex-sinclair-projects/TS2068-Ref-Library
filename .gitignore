# ---- macOS -----------------------------------------------------------------
.DS_Store
**/.DS_Store

# ---- Claude Code -----------------------------------------------------------
# Machine-local settings only. A shared .claude/settings.json, if one is ever
# added, is deliberately NOT ignored.
.claude/settings.local.json

# ---- TS-PICO material ------------------------------------------------------
# Kept locally, not redistributed. Note that this means a fresh clone will not
# contain tspico/, even though CLAUDE.md documents it.
/tspico
gus-exrom.asm
gus-exrom.rom
gus-home.asm
gus-home.rom
gus-rom-analysis.md
TSPICO-ASM-INT-DIST-2023.pdf
tspico-d-error-code-context.txt
tspico-d-error-team-summary.txt
